# Copy this file to `.env` and fill in your own value.
# `.env` is git-ignored and must NEVER be committed.
#
# Google Cloud project ID that hosts the BigQuery dataset (`<project>.fraud.train_zengin`).
GCP_PROJECT_ID=silver-link-430412-g6
